// Import the token handling functions
//import { saveToken, loadToken, removeToken } from './token.js';
/*
// Example usage after successful registration or login
const receivedToken = 'YOUR_RECEIVED_TOKEN_FROM_API';
saveToken(receivedToken);

// Later, for sending the token with API requests
const tokenToSend = loadToken();

// When logging out
removeToken();
*/
